 # ── mode ──────────────────────────────────────
    "BITS",          # bits 16 / 32 / 64
    "USE16",         # 16-bit mode
    "USE32",         # 32-bit mode
    "DEFAULT",       # default rel/abs

    # ── sections ──────────────────────────────────
    "SECTION",       # section .text
    "SEGMENT",       # same as section
    "ABSOLUTE",      # absolute section

    # ── symbols ───────────────────────────────────
    "GLOBAL",        # export symbol
    "EXTERN",        # import external symbol
    "COMMON",        # common symbol
    "STATIC",        # static symbol
    "LOCAL",         # local symbol

    # ── data definition ───────────────────────────
    "DB",            # define byte      (1 byte)
    "DW",            # define word      (2 bytes)
    "DD",            # define dword     (4 bytes)
    "DQ",            # define qword     (8 bytes)
    "DT",            # define tword     (10 bytes)
    "DO",            # define oword     (16 bytes)
    "DY",            # define yword     (32 bytes)
    "DZ",            # define zword     (64 bytes)

    # ── uninitialized data ────────────────────────
    "RESB",          # reserve bytes
    "RESW",          # reserve words
    "RESD",          # reserve dwords
    "RESQ",          # reserve qwords
    "REST",          # reserve twords
    "RESO",          # reserve owords
    "RESY",          # reserve ywords
    "RESZ",          # reserve zwords

    # ── constants ─────────────────────────────────
    "EQU",           # constant definition  NAME EQU value
    "TIMES",         # repeat              times 10 db 0

    # ── alignment ─────────────────────────────────
    "ALIGN",         # align to boundary
    "ALIGNB",        # align in BSS section

    # ── macros ────────────────────────────────────
    "MACRO",         # define macro
    "ENDMACRO",      # end macro
    "%MACRO",        # define macro (% prefix style)
    "%ENDMACRO",     # end macro
    "%DEFINE",       # simple macro define
    "%UNDEF",        # undefine macro
    "%ASSIGN",       # numeric macro assign
    "%STRLEN",       # string length macro

    # ── conditionals ──────────────────────────────
    "%IF",           # if condition
    "%ELIF",         # else if
    "%ELSE",         # else
    "%ENDIF",        # end if
    "%IFDEF",        # if defined
    "%IFNDEF",       # if not defined
    "%IFMACRO",      # if macro defined

    # ── includes ──────────────────────────────────
    "%INCLUDE",      # include file
    "%INCBIN",       # include binary file

    # ── output format ─────────────────────────────
    "ORG",           # set origin address
    "CPU",           # set cpu type
    "FLOAT",         # floating point definition

    # ── struct ────────────────────────────────────
    "STRUC",         # define structure
    "ENDSTRUC",      # end structure
    "ISTRUC",        # instance of structure
    "AT",            # structure field offset
    "IEND",          # end structure instance

    # ── procedures ────────────────────────────────
    "PROC",          # procedure start
    "ENDPROC",       # procedure end

    # ── warnings / errors ─────────────────────────
    "%WARNING",      # emit warning
    "%ERROR",        # emit error
    "%FATAL",        # emit fatal error
