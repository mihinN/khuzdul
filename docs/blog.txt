this is project that combined with assembler + shellcode engine

assembler need 
raw_code -> lexer (tokenize) -> ir layer -> parser -> encoder -> generates_flat_binary 

lexer basically tokenize , inputs
IR layer works as manipulation layer and that connects to the parser 
parser understand those tokens and add meaning to that. 
in here those tokens might be a register , address , label , we have to store those 
into ast (abstract sysntax tree) and encode it
in intelx86 encodes into 15byes, not using whole 15bytes , currently available any instructions. 

in parser we have to add symbols to the labels

lexer 
parser (ast , sementic analysis)

raw_assembly__tokens -> ast -> sementic analysis -> 

tokens stores in the AST 
so what are other data structures that are sued to stores tokens while parsing ? 
tree based data structures 
    ast 
    cst 
    dag 

graph based 
    CFG
    DFG 
    SSA 

linear flat 
    Token stream / Token list 
    Bytecode (linear address list)
    Three address code (TAC)

Table based 
    Symbol table 
    String inten table 
    Type table 


what LLMs use to store tokens , when it tokenize inthe first hand 
Raw Text → Token IDs (just flat integer array)
Token IDs → Embedding Matrix (high dimensional vectors)
Embeddings → Attention Matrix

Text
  ↓
Flat Integer Array     ← token IDs
  ↓
Embedding Matrix       ← vectors
  ↓
Attention Matrix       ← relationships (Q, K, V)
  ↓
Output Vectors
  ↓
Predicted Token


Compiler      AI Model
AST, CST, CFG   Tensors, Matrices
Symbolic, discrete      Statistical, continuous
Rule based          pattern based 
Deterministic       Probabilistics 

How Humans needs computers? 
solving problems -> high level languages -> compilers -> ISA -> micro arch -> 
micro operation -> logic gates -> transistors -> transistor logics .. 

money and power ? 
nations and leader like money 
open architecture chip design align with national security

open source ->


world make a batter place?? 
how to improve civilization -> harmony , peace , education , culture

what actually helps to people , and people buy it 
two parts -> actually needed (needs), they like (entertainment , desire)
what people need : peace , lust , desires like ()


product philosopher 
desire : loud 
needs : silent 

Find the core need → understand how it is currently being met → find where it is still poorly served → improve the root, not the decoration.
no biased , no cheerleading 



 thought for shellcode engine 
 - mapping the system and write generate or give information to write good shellcode
 - not just generating , give more advance information about process , memory spaceses 
 is there any interprocess communications , knows vuln like things to build advanced percise 
 shellcode 



- How vector Instructions used to Obfuscated the shellcode/payload 
because most of the time , shellcodes use integer instructions , yes , it has their owne reasons like 
syscalls are encoded as integers , but you can use vector instructions creativiely. 
in other hand , wherethe you cretive or not , EDR detects when you call execv "bin/shell" like instructions/commands